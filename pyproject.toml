[tool.poetry]
name = "xpath"
version = "0.1.0"
description = "xpath copy"
authors = ["KGerring <kgerring@gmail.com>"]

[tool.poetry.dependencies]
python = "~3"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
